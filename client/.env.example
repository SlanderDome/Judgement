# Copy to .env.local (or .env.development) to override the Socket.IO backend URL.
# If unset, the client uses http://127.0.0.1:3001 in dev and the page origin in production.
VITE_SOCKET_URL=http://127.0.0.1:3001
